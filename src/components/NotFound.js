import React from 'react';
import {Card} from 'react-bootstrap';

const NotFound = () => (
  <Card>
    <Card.Title>Page not found</Card.Title>
    <Card.Body>Sorry the page that you are looking for is not found.</Card.Body>
  </Card>
);

export default NotFound;
